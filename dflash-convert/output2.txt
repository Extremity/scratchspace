Yes. Exactly. You caught the distinction, and my last response muddied it.

We already established:

Original bootstrap GGUF — good.

fc.weight = [25600, 5120]

Freshly recreated bootstrap GGUF — also good.

fc.weight = [25600, 5120]

The problem was the subsequent GGUF → safetensors conversion.
The resulting:

model.safetensors
fc.weight = [5120, 51200]

is wrong.

So recreating the bootstrap was not necessary to fix the actual problem. We did it only because you suggested it was cheap and it gives us a fresh, unquestionably-good source. That's fine, but the original bootstrap was already valid.

More importantly, we don't need to investigate the original bootstrap any further. We've already proven it.

The actual job now is:

GOOD Qwen3.8 bootstrap GGUF
        ↓
CORRECT HF/safetensors representation
        ↓
training

And the immediate thing we need to solve is the middle arrow.

Also, I don't want to make you run random commands to rediscover what happened. The old model.safetensors was created August 26, so if we don't have the exact conversion command readily available, we can simply identify the appropriate current conversion path and validate its output tensor-for-tensor against the known-good GGUF.

Next steps: keep Qwen3.8-27B-DFlash-bootstrap-bf16-fresh.gguf untouched. We should now focus exclusively on getting that known-good GGUF into the correct Speculators/PyTorch checkpoint format.

I dont have much space, do we need both of these GGUF files

No — you do not need both.

The newly created:

Qwen3.8-27B-DFlash-bootstrap-bf16-fresh.gguf

is confirmed identical in the important structural respect to the original:

Qwen3.8-27B-DFlash-bootstrap-bf16.gguf

Both have:

fc.weight = [25600, 5120]

The fresh one is now our verified replacement. Once we've confirmed the fresh file exists and is good—which we just did—you can delete the original bootstrap GGUF to reclaim ~3.47 GB.

I'd keep the fresh one and delete:

rm ~/data/drafters/Qwen3.8-27B-DFlash-bootstrap-bf16.gguf

Don't delete Qwen3.8-dflash-draft-hf/ yet; that's the bad 6.5 GB safetensors checkpoint, but we may want to inspect/replace it as part of the next conversion step.

So your storage situation becomes:

KEEP
Qwen3.8-27B-DFlash-bootstrap-bf16-fresh.gguf   ~3.47 GB

DELETE
Qwen3.8-27B-DFlash-bootstrap-bf16.gguf         ~3.47 GB

And importantly, the fresh GGUF is now the only bootstrap artifact we need going forward.