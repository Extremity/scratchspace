# DATA SPECIFICATION: Qwen3.8-27B DFlash Speculative Distillation Bin
**File Name:** `dflash_alignment_targets.bin`  
**Total File Size:** `17.41 GB` (Binary Continuous Stream)  
**Target Teacher Model:** Qwen 3.8-27B Q8_0 GGUF (`n_vocab = 248320`, `hidden_dim = 5120`)  
**Extracted Brain Layers:** 5 Layers Total (`2, 17, 32, 47, 62`)  
**Extraction Methodology:** Generation-Based (Anthropic/SLM approach via `--generate 64`)  

---

## 💾 1. General Storage Architecture
Unlike traditional JSON or text datasets, this file contains **zero text formatting, zero whitespace, and zero string tokens**. It is a packed, byte-aligned binary data stream generated natively via C++ memory writes (`std::ostream::write`). 

The document is divided into **118 continuous blocks**. Each block contains an explicit numeric byte-header followed by its multi-dimensional array payloads written back-to-back.

---

## 🧱 2. Block-by-Block Binary Layout Syntax

Every individual data block inside the stream strictly adheres to the following structural memory layout (read from left to right):

### 📥 [Part A: The Sequence Header]
*   **Size:** 4 Bytes  
*   **Data Type:** 32-bit Signed Integer (`i32` / Python struct format `=i`)  
*   **Syntax Definition:** Specifies the **exact number of tokens (`N`)** contained in the current hidden state chunk matrix. 
*   *Note:* In this raw batch extraction pass, `N` acts as a multi-prompt accumulator (e.g., Block 1 contains `1499` stacked token paths tightly compressed together).

### 🧠 [Part B: Stream A - Hidden States Array]
*   **Size:** `5 (Layers) * N (Tokens) * 5120 (Hidden Dim) * 4 Bytes`  
*   **Data Type:** 32-bit Floating Point Array (`f32` / Python struct format `=f`)  
*   **Syntax Definition:** A flattened, multi-dimensional array containing the raw activation vectors captured from Qwen 3.8's internal layers. 
*   **Reshape Dimension Geometry:** `[5, N, 5120]`
*   *Training Utility:* These are the **Input features** for your 5-layer DFlash speculator head.

### 🎯 [Part C: Stream B - Target Vocabulary Logits]
*   **Size:** `64 (Generated Tokens) * 248320 (Vocab Size) * 4 Bytes`  
*   **Data Type:** 32-bit Floating Point Array (`f32` / Python struct format `=f`)  
*   **Syntax Definition:** A flattened, continuous probability distribution matrix generated by the model's absolute final output classification head during the 64-token autoregressive generation loop.
*   **Reshape Dimension Geometry:** `[64, 248320]`
*   *Training Utility:* This is the **Ground-truth optimization target** used to calculate the cross-entropy loss function during fine-tuning.

---

## ⚙️ 3. Native Python/PyTorch Binary Slicing Logic

If your data pipeline drifts out of alignment in the future, you can parse or verify this exact byte mapping structure using standard Python `struct` unpackers. 

The sequential progression logic for every block matches this exact Python math:

```python
# Unpack Header (First 4 Bytes)
tokens_in_block = struct.unpack("=i", f.read(4))[0]

# Calculate exact byte bounds dynamically
hidden_state_bytes = 5 * tokens_in_block * 5120 * 4
logit_target_bytes = 64 * 248320 * 4

# Slice the data streams cleanly
raw_hidden_states = f.read(hidden_state_bytes)
raw_target_logits = f.read(logit_target_bytes)

# Shift pointer directly to the start of the next block header
```

---

## ⚠️ 4. Key Compatibility & Integration Quirks

*   **Zero-Copy Memory Mapping Mandatory:** Because the file is a massive 17.41 GB block, you should **never** use standard loop-reading methods like `f.read()`. Your PyTorch datasets must load this natively using **`np.memmap(..., mode='r')`**. This maps the array directly to your NVMe swap files, keeping system RAM overhead near zero.
*   **End-of-File Integrity Check:** The file concludes directly on the final byte of the 118th block's logit distribution array. If the total byte size of the file divided by the standard block calculation leaves a remainder, it indicates a partial file flush or an interrupted execution pass.
*   **Vocabulary Hard-Lock:** The target logit dimensions are completely hard-locked to Qwen 3.8's exact vocabulary space (`248,320` choices). If you attempt to use this binary file on a newer Qwen model build (e.g., a Qwen 3.5 or Qwen 4 base model that features a different tokenizer dictionary setup), the matrices will experience shape-mismatch collapse during the training pass.
